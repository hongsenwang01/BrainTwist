# 成就系统接入说明

## 前端现状

成就页脚本：`assets/scripts/成就/AchievementSceneView.ts`

本次已把成就列表改成数据驱动：

- 本地默认只保留 1 条成就：`初来乍到`
- 页面启动后会读取 `brain_twist_user_id`
- 页面会先读取本地缓存并立即渲染，不会每次打开都强制等待接口
- 无缓存、缓存过期、或游戏总结上传成功后标记缓存过期时，才请求后端成就列表接口
- 后端返回多少条，页面就按同一个行渲染逻辑向下列出多少条
- 页面会保留 Cocos 场景里已经摆好的静态 UI，优先复用一个通用模板节点 `成就条模板` 生成列表；如果没有这个节点，会兼容旧的 `成就条-一击即中` 示例行作为模板
- 请求失败时默认显示本地 `初来乍到`，便于后端未完成时继续调 UI

## 缓存策略

当前采用“本地缓存 + 脏标记 + 短 TTL”的方式，避免频繁请求接口：

1. 成就页打开时，优先读取 `localStorage.brain_twist_achievement_cache_v1`，有缓存就立即渲染。
2. 如果没有缓存，或者缓存超过 `缓存有效期秒`，才请求 `/api/achievements`。
3. 游戏总结上传成功后，会写入 `localStorage.brain_twist_achievement_cache_dirty = "1"`。
4. 下次打开成就页时，如果发现 dirty 标记，会请求一次后端，刷新本地缓存并清除 dirty 标记。
5. 如果玩家只是反复打开成就页，没有打完新一局，也没有超过缓存时间，就不会重复请求接口。

这样能达到两个目标：

- 成就页打开快：先展示本地缓存，不等接口。
- 进度足够实时：每局游戏总结上传成功后，下次打开成就页一定刷新一次。

如果后续希望“游戏总结页立刻弹出新解锁成就”，可以让 `/api/game-scores/submit` 在响应里额外返回 `changedAchievements`，前端直接更新缓存并展示弹窗；成就页仍然使用当前缓存机制。

## Cocos 配置

打开 `assets/成就.scene`，选中挂载 `AchievementSceneView` 的节点，Inspector 配置：

| 配置项 | 建议值 | 说明 |
| --- | --- | --- |
| 自动加载后端成就 | 勾选 | 页面进入后自动请求接口 |
| 后端服务地址 | `http://localhost:3000` | 和登录、游戏总结接口保持一致；上线改成正式域名 |
| 成就列表接口路径 | `/api/achievements` | GET 接口 |
| 游戏标识 | `reverse_brain` | 和游戏总结上传使用同一个 `gameKey` |
| 游戏模式 | `classic` | 和游戏总结上传使用同一个 `gameMode` |
| 请求失败显示默认列表 | 开发期勾选 | 后端未完成或异常时显示 `初来乍到` |
| 使用本地成就缓存 | 勾选 | 打开成就页时先展示缓存，减少接口请求 |
| 缓存有效期秒 | `300` | 没有新游戏结果时，5 分钟内重复打开不重新请求 |

模板节点要求：

- 推荐只保留一个通用模板节点：`成就条模板`
- `成就条模板` 下放这些子节点：
  - `图标`
  - `标题`
  - `描述`
  - `进度槽`
  - `进度填充`
  - `进度文本`
  - `奖励`
  - `奖励数量`
  - `状态`
- 模板节点可以放在场景里任意位置，脚本启动后会自动隐藏，只用于 `instantiate` 生成列表。
- `showProgress = false` 的成就会自动隐藏 `进度槽`、`进度填充`、`进度文本`。

兼容旧场景：

- 如果暂时不建 `成就条模板`，脚本会继续使用旧的 `成就条-一击即中`、`图标-一击即中`、`标题-一击即中` 等示例节点作为模板。
- 旧的 6 条示例节点可以保留，运行时会被自动隐藏。

静态节点要求：

- 总进度节点：`已完成数量`、`总数量`、`总进度槽`、`总进度填充`、`总进度百分比`
- 分类节点：`分类选中`、`分类-全部`、`分类-成长`、`分类-挑战`、`分类-反应`、`分类-收集`、`分类-隐藏`

资源要求：

- `assets/resources/textures/achievements` 下需要保留这些 SpriteFrame 名称：
  - `face-pink-wink`
  - `reward-coin`
  - `reward-gem`
  - `status-check`
  - `status-lock`
  - `trophy-purple`
  - `mascot-header-pink`
  - `mascot-header-purple`
  - `back-arrow-pink`
- 后端返回的 `icon` 必须是该目录下已有 SpriteFrame 的名称；没有命中时前端会默认使用 `face-pink-wink`。

依赖前置：

- 登录页已有 `/api/auth/douyin-login`
- 登录成功后前端会写入 `sys.localStorage.brain_twist_user_id`
- 成就页使用这个 `userId` 拉取用户成就进度

## 成就列表接口

请求：

```http
GET /api/achievements?userId={userId}&gameKey=reverse_brain&gameMode=classic
```

成功响应：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "unlockedCount": 1,
    "totalCount": 1,
    "achievements": [
      {
        "id": "tutorial",
        "category": "growth",
        "title": "初来乍到",
        "description": "完成新手教程",
        "icon": "face-pink-wink",
        "rewardKind": "coin",
        "rewardAmount": 10,
        "current": 1,
        "target": 1,
        "progressText": "1/1",
        "unlocked": true,
        "showProgress": false
      }
    ]
  }
}
```

字段说明：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| id | string | 是 | 成就唯一 key，例如 `tutorial` |
| category | string | 是 | `growth`、`challenge`、`reaction`、`collect`、`hidden` |
| title | string | 是 | 成就名称 |
| description | string | 是 | 成就描述 |
| icon | string | 是 | Cocos SpriteFrame 名称 |
| rewardKind | string | 是 | `coin` 或 `gem` |
| rewardAmount | number | 是 | 奖励数量 |
| current | number | 是 | 当前进度 |
| target | number | 是 | 目标进度，最小为 1 |
| progressText | string | 否 | 展示文本，例如 `6/10h`；不传则前端显示 `current/target` |
| unlocked | boolean | 是 | 是否已解锁 |
| showProgress | boolean | 否 | 是否显示进度条；不传时未解锁默认显示 |

失败响应：

```json
{
  "code": 10001,
  "message": "userId is required"
}
```

## MySQL 建表建议

如果后端使用 MySQL 8，可以先建这两张表。成就定义和用户进度分开，方便后续新增成就，不需要改前端。

```sql
CREATE TABLE achievement_definitions (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  achievement_key VARCHAR(64) NOT NULL,
  game_key VARCHAR(64) NOT NULL DEFAULT 'reverse_brain',
  game_mode VARCHAR(64) NOT NULL DEFAULT 'classic',
  category VARCHAR(32) NOT NULL,
  title VARCHAR(64) NOT NULL,
  description VARCHAR(255) NOT NULL,
  icon VARCHAR(64) NOT NULL DEFAULT 'face-pink-wink',
  reward_type VARCHAR(16) NOT NULL DEFAULT 'coin',
  reward_amount INT UNSIGNED NOT NULL DEFAULT 0,
  target_value DECIMAL(12, 2) NOT NULL DEFAULT 1,
  condition_key VARCHAR(64) NOT NULL,
  sort_order INT NOT NULL DEFAULT 0,
  is_hidden TINYINT(1) NOT NULL DEFAULT 0,
  is_enabled TINYINT(1) NOT NULL DEFAULT 1,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_achievement_key (achievement_key),
  KEY idx_game_category (game_key, game_mode, category, sort_order)
);

CREATE TABLE user_achievements (
  id BIGINT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY,
  user_id VARCHAR(64) NOT NULL,
  achievement_id BIGINT UNSIGNED NOT NULL,
  current_value DECIMAL(12, 2) NOT NULL DEFAULT 0,
  unlocked TINYINT(1) NOT NULL DEFAULT 0,
  unlocked_at DATETIME NULL,
  reward_claimed TINYINT(1) NOT NULL DEFAULT 0,
  claimed_at DATETIME NULL,
  created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY uk_user_achievement (user_id, achievement_id),
  KEY idx_user_unlocked (user_id, unlocked),
  CONSTRAINT fk_user_achievements_definition
    FOREIGN KEY (achievement_id) REFERENCES achievement_definitions(id)
);
```

初始数据：

```sql
INSERT INTO achievement_definitions (
  achievement_key,
  game_key,
  game_mode,
  category,
  title,
  description,
  icon,
  reward_type,
  reward_amount,
  target_value,
  condition_key,
  sort_order,
  is_hidden,
  is_enabled
) VALUES (
  'tutorial',
  'reverse_brain',
  'classic',
  'growth',
  '初来乍到',
  '完成新手教程',
  'face-pink-wink',
  'coin',
  10,
  1,
  'tutorial_complete',
  10,
  0,
  1
);
```

## 后端接口实现思路

伪代码：

```ts
app.get("/api/achievements", async (req, res) => {
  const { userId, gameKey = "reverse_brain", gameMode = "classic" } = req.query;

  if (!userId) {
    return res.json({ code: 10001, message: "userId is required" });
  }

  const definitions = await db.query(`
    SELECT *
    FROM achievement_definitions
    WHERE game_key = ?
      AND game_mode = ?
      AND is_enabled = 1
    ORDER BY sort_order ASC, id ASC
  `, [gameKey, gameMode]);

  const progressRows = await db.query(`
    SELECT *
    FROM user_achievements
    WHERE user_id = ?
  `, [userId]);

  const progressMap = new Map(progressRows.map(row => [row.achievement_id, row]));

  const achievements = definitions.map(def => {
    const progress = progressMap.get(def.id);
    const current = Number(progress?.current_value ?? 0);
    const target = Math.max(1, Number(def.target_value));
    const unlocked = Boolean(progress?.unlocked);

    return {
      id: def.achievement_key,
      category: def.category,
      title: def.title,
      description: def.description,
      icon: def.icon,
      rewardKind: def.reward_type,
      rewardAmount: Number(def.reward_amount),
      current,
      target,
      progressText: `${Math.min(current, target)}/${target}`,
      unlocked,
      showProgress: !unlocked
    };
  });

  res.json({
    code: 0,
    message: "ok",
    data: {
      unlockedCount: achievements.filter(item => item.unlocked).length,
      totalCount: achievements.length,
      achievements
    }
  });
});
```

## 和游戏总结接口联动

已有游戏总结上传接口：

```http
POST /api/game-scores/submit
```

建议在该接口保存成绩后，同步调用成就进度计算逻辑：

```ts
async function updateAchievementsByGameSummary(userId: string, summary: GameSummaryPayload) {
  await unlockOrUpdate(userId, "tutorial", 1);

  // 后续新增成就示例：
  // await unlockOrUpdate(userId, "ten_streak", summary.maxCombo);
  // await unlockOrUpdate(userId, "perfect_round", summary.accuracy >= 100 ? 1 : 0);
  // await unlockOrUpdate(userId, "play_10_rounds", userTotalRounds);
}
```

`unlockOrUpdate` 规则：

- 找到 `achievement_definitions.achievement_key`
- upsert 到 `user_achievements`
- `current_value` 建议取历史最大值，累计类成就按累计值写入
- `current_value >= target_value` 时设置 `unlocked = 1` 和 `unlocked_at`
- 已解锁成就不要重复覆盖  unlocked_at`

```ts
async function unlockOrUpdate(userId: string, achievementKey: string, currentValue: number) {
  const def = await findAchievementDefinition(achievementKey);
  if (!def) return;

  const nextValue = Math.max(0, currentValue);
  const unlocked = nextValue >= Number(def.target_value);

  await db.query(`
    INSERT INTO user_achievements (
      user_id,
      achievement_id,
      current_value,
      unlocked,
      unlocked_at
    ) VALUES (?, ?, ?, ?, ?)
    ON DUPLICATE KEY UPDATE
      current_value = GREATEST(current_value, VALUES(current_value)),
      unlocked = IF(unlocked = 1, 1, VALUES(unlocked)),
      unlocked_at = IF(unlocked_at IS NOT NULL, unlocked_at, VALUES(unlocked_at)),
      updated_at = CURRENT_TIMESTAMP
  `, [
    userId,
    def.id,
    nextValue,
    unlocked ? 1 : 0,
    unlocked ? new Date() : null
  ]);
}
```

## 后续新增成就流程

1. 在 `achievement_definitions` 插入新成就定义。
2. 后端在登录、教程完成、游戏总结上传等节点更新 `user_achievements`。
3. 成就页无需改代码，接口返回新数据后自动展示。
4. 如果新成就需要新图标，把图片放到 `assets/resources/textures/achievements`，并让接口 `icon` 返回对应 SpriteFrame 名称。

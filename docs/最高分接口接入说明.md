# 最高分接口接入说明

## 前端位置

首页最高分节点：`assets/游戏首页.scene` 里的 `02-05最高分/分数`

当前该节点挂载的是 `RandomScoreLabel`。已调整为：

- `randomizeOnStart = false`
- `fetchBestScoreOnStart = true`
- `bestScoreApiPath = /api/game-scores/best`
- `gameKey = reverse_brain`
- `gameMode = classic`

进入首页后会先读本地缓存 `brain_twist_best_score_cache_v1`，再按需请求最高分接口。游戏总结上传成功后，如果后端返回 `bestScore`，前端会同步更新这个缓存。

## 请求接口

```http
GET /api/game-scores/best?userId={userId}&gameKey=reverse_brain&gameMode=classic
```

字段说明：

| 字段 | 位置 | 必填 | 说明 |
| --- | --- | --- | --- |
| userId | query | 是 | 登录接口返回并写入本地的用户 ID |
| gameKey | query | 是 | 当前为 `reverse_brain` |
| gameMode | query | 是 | 当前为 `classic` |

成功响应：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "bestScore": 464
  }
}
```

失败响应：

```json
{
  "code": 10001,
  "message": "userId is required"
}
```

## 后端实现建议

如果已有成绩表，例如 `game_score_records`，可以直接按用户、游戏、模式查询最高分。

```ts
app.get("/api/game-scores/best", async (req, res) => {
  const { userId, gameKey = "reverse_brain", gameMode = "classic" } = req.query;

  if (!userId) {
    return res.json({ code: 10001, message: "userId is required" });
  }

  const row = await db.queryOne(`
    SELECT COALESCE(MAX(score), 0) AS bestScore
    FROM game_score_records
    WHERE user_id = ?
      AND game_key = ?
      AND game_mode = ?
  `, [userId, gameKey, gameMode]);

  return res.json({
    code: 0,
    message: "ok",
    data: {
      bestScore: Number(row?.bestScore ?? 0)
    }
  });
});
```

建议索引：

```sql
CREATE INDEX idx_game_score_user_best
ON game_score_records (user_id, game_key, game_mode, score);
```

如果后端已经在用户统计表里维护最高分，也可以直接查统计表：

```sql
SELECT best_score AS bestScore
FROM user_game_stats
WHERE user_id = ?
  AND game_key = ?
  AND game_mode = ?;
```

## 和游戏总结接口的关系

已有：

```http
POST /api/game-scores/submit
```

该接口建议继续返回：

```json
{
  "code": 0,
  "message": "ok",
  "data": {
    "roundId": "round_001",
    "isNewBest": true,
    "bestScore": 464
  }
}
```

前端会在收到 `bestScore` 后写入本地缓存。这样玩家打完一局回到首页时，即使最高分接口还没请求完成，也能先看到最新最高分。
